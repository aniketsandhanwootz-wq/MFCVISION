Display hint: __DISPLAY_KIND_LABEL__.
You have TWO images: PRIMARY = localized display crop, CONTEXT = full original photo.

TASK
First decide whether PRIMARY is a trustworthy display crop. Then read the value from the strongest valid source.

SOURCE SELECTION
- VALID PRIMARY crop: an actual display window with visible digit structure.
- INVALID PRIMARY crop: bowl, pan, rim, buttons, casing, labels, blank strip, or blur with no readable display row.
- If PRIMARY is invalid, discard it and read from CONTEXT only.

READING RULES
- LED: read only bright active segments. Dim full-width 8-shaped outlines are placeholders unless clearly as bright as the active digits.
- LED digit 1 rule: a narrow bright right-side column can be a real 1. Do not skip it just because it is narrow.
- LED edge rule: if the leftmost apparent digit is clipped by the crop border, use CONTEXT before deciding whether it is 1 or a clipped wider digit.
- LCD: read only dark digit strokes inside the screen rectangle. Ignore device body, labels, and buttons.
- Include the decimal only if a small isolated dot is clearly visible.
- Return exactly one numeric token: digits with at most one decimal point.
- Never return placeholder separators such as 8.8.670 or any token with multiple decimal points.
