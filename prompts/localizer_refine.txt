Role: display-window localizer refinement only. Do not read digits.
You are verifying and correcting a previous display-window box proposal.

DISPLAY FAMILY HINT
__DISPLAY_FAMILY_HINT__

Previous proposal (normalized 0..1000):
x1=__X1__, y1=__Y1__, x2=__X2__, y2=__Y2__, kind=__DISPLAY_KIND__, confidence=__CONFIDENCE__.

TASK
Return a corrected box around the full physical display window or screen only.

REFINEMENT RULES
- If the previous box includes bowl rim, metal ring, blue strip, labels, branding, buttons, device face without screen, or only part of the display, correct it.
- For LED devices, box the full dark display panel with the full display row inside.
- For LCD devices, box the full inner screen rectangle and stop before buttons below it.
- The box must not span the full image width.
- The box must be wider than tall and must not exceed 30% of image height.
- For LED scales, prefer the lower half of the image over the bowl area.
- On bowl scales, the correct target is the black inset display window on the blue fascia. Do not return the whole blue fascia and do not include the round buttons below it.
- If two large horizontal regions are visible, the correct display is the narrow dark window between the branding strip and the buttons, not the bowl/platform above and not the control panel below.
- If the current box contains the full weighing platform or the full lower front body, shrink it aggressively to the inner display window.
- If the display is genuinely not localizable, return found=false rather than a bad partial box.

OUTPUT RULES
- Coordinates are integers normalized 0..1000 relative to the full image.
- display_kind must be led, lcd, or unknown.
- is_complete_window: true only if the FULL physical display window is boxed.
- left_clipped / right_clipped: true if the proposed window still looks cut off on that side.
- wrong_region_risk: low, medium, or high based on the chance the box is still on a non-display region.
- reason must describe location only and never mention an inferred reading.
